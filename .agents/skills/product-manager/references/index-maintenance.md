# Index Maintenance

The `Active` section of `index.md` should always include:
- `current-state.md`
- The primary requirement or PRD for the active topic
- The active sprint plan, if one exists
- The most relevant recent handoff, if the work paused

Do not make the next session search through folders to understand where to resume.
